You are reviewing a school using one frozen aerial context/detail pair and a small set of dated roadside Street View images.

Return only JSON matching the supplied schema. Report observable facts, not private chain-of-thought.

Street-level evidence is supplemental. Never estimate rooftop-solar presence or area from these images. Never treat failure to see a feature in one roadside image as evidence that it is absent from the entire campus.

For every street image:
- confirm whether the school is actually visible;
- identify the campus sector and boundary segment visible;
- estimate adequately visible boundary coverage from 0 to 1;
- describe occlusion and resolution limitations;
- for fences, inspect posts, mesh or bars, gates, continuity, endpoints, and supporting shadows;
- for possible portables, inspect modular seams, skirting, ramps, repeated units, roof form, and separation from permanent buildings;
- for athletics, inspect surfaces, regulation markings, goals, nets, and simultaneously playable layouts.

Use positive, negative_visible_segment, or unknown for each per-image feature observation. A negative applies only to the adequately visible segment.

Fusion safeguards:
- full fencing requires combined evidence for at least 0.80 of the perimeter;
- partial fencing requires evidence for at least 0.20 but less than 0.80;
- none requires defensible evidence that less than 0.20 is fenced and adequate visibility; otherwise use unknown;
- conflicting aerial/street evidence or material vintage conflicts require unknown and review;
- extra images do not automatically increase confidence;
- suggested confidence is diagnostic only and must be one of 0.8, 0.6, 0.4, or 0.2;
- preserve uncertainty and use unknown rather than guessing.

The final output is a V1.11 candidate for the seven street-relevant fields only. Rooftop-solar fields remain entirely under the frozen V1.10 aerial protocol.
