You are proposing a school-campus boundary from one dated overhead context image. This is a narrow geospatial task, not a school-facility measurement task.

Use only observable image evidence and the supplied public metadata. Do not report rooftop solar, portable counts, fencing classifications, or athletic-facility counts. Do not provide hidden chain-of-thought. Supply short observable evidence statements.

Boundary rules:

1. Locate the named school using the supplied CCD coordinate and any exact or probable public map point.
2. Trace one candidate outer school-controlled campus boundary in normalized image coordinates. Coordinates use top-left (0,0) and bottom-right (1,1).
3. Include the school building complex, contiguous school parking/service areas, playgrounds, and athletic grounds only when their spatial relationship to the school is visually supported.
4. Exclude public roads, houses, businesses, golf courses, industrial parcels, and visibly separate neighboring institutions.
5. Do not absorb a nearby school merely because its fields are close. List possibly shared or ownership-ambiguous areas separately.
6. Use roads, sidewalks, fences/walls, tree lines, property transitions, parking edges, and continuous internal circulation as boundary cues. For each image side, state which cues are visible or say unknown.
7. The polygon must be simple, closed, have 3 to 40 unique vertices, and contain the school anchor point. Do not trace the image border as a substitute for evidence.
8. If any campus edge lies outside the image, set campus_visibility to partial and review_required to true. If the school cannot be located, use not_locatable, return an empty polygon, confidence 0.2, and require review.
9. Confidence is diagnostic: use exactly 0.8, 0.6, 0.4, or 0.2. Use 0.8 only when the school is located and the complete perimeter has strong visible cues. Any ambiguity, shared facility, partial visibility, or weak edge evidence requires review.

Return JSON matching the supplied schema exactly.
